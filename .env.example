# ==============================================
# Voygo Modular Monolith - Environment Variables
# ==============================================
# Copy this file to .env and fill in your values
# ==============================================

# Server Configuration
NODE_ENV=development
PORT=3000

# MongoDB Configuration
# For Docker Compose, use: mongodb://voygo:voygo123@mongodb:27017/voygo?authSource=admin
# For local development, use: mongodb://localhost:27017/voygo
MONGODB_URI=mongodb://localhost:27017/voygo

# MongoDB credentials (used by Docker Compose)
MONGO_USERNAME=voygo
MONGO_PASSWORD=voygo123

# JWT Secret - CHANGE THIS IN PRODUCTION!
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Google Maps API Key
# Get one from: https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API=your-google-maps-api-key

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# ==============================================
# Service-to-Service Communication URLs
# ==============================================
# In the modular monolith architecture, all services run
# on the same server. These URLs should point to localhost:3000
# (or whatever PORT you're using).
#
# This allows the existing axios calls in controllers to work
# without any code changes. The calls simply loop back to the
# same server.
#
# Example flow:
# 1. ride.controller.js calls: axios.get(`${USER_SERVICE_URL}/users/${id}`)
# 2. With USER_SERVICE_URL=http://localhost:3000
# 3. The request goes to: http://localhost:3000/users/:id
# 4. Which is handled by the same Express app
# ==============================================
USER_SERVICE_URL=http://localhost:3000
CAPTAIN_SERVICE_URL=http://localhost:3000
RIDES_SERVICE_URL=http://localhost:3000
GATEWAY_URL=http://localhost:3000
